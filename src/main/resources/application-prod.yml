notification:
    domain: https://washing-time.herokuapp.com/app
spring:
    datasource:
        driver-class-name: org.postgresql.Driver
        url: ${SPRING_DATASOURCE_URL}
        username: ${SPRING_DATASOURCE_USERNAME}
        password: ${SPRING_DATASOURCE_PASSWORD}
dirty:
    clothes:
        notification:
            scheduling: 120000
            deviation: 60000
            enabled: true
            src-path: https://backend-for-react-resource.herokuapp.com/resource
logging:
    level:
        org:
            springframework:
                security: ERROR